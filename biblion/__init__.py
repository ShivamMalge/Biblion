"""Biblion — turn plain markdown into a typeset PDF book.

The point of this package is token economy: you pay an AI once to write
CONTENT-ONLY markdown (prose, tables, ```mermaid / ```d2 diagram source),
and Biblion does all the layout, typography, pagination and diagram
rendering locally, for free, on every subsequent run.
"""

__version__ = "0.3.0"
